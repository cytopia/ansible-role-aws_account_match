---

- hosts: all
  connection: local
  vars:
    aws_account_match_allowed_account: 12345
  roles:
    - aws-account-match
  tags:
    - aws-account-match

- hosts: all
  connection: local
  vars:
    aws_account_match_allowed_accounts: [012345, 123456, 234567]
  roles:
    - aws-account-match
  tags:
    - aws-account-match
